{% extends "base.html.twig" %}

{% block title %}Wild series{% endblock %}

{% block body %}
    <h1>{{ season.description }} de {{ program.title }}</h1>
    <p>{{ program.summary }}</p>
    <h2>Liste des épisodes :</h2>
    <ul>
        {% for episode in season.episodes %}
        <li>
                <a href="{{ path('program_episode_show', { program_slug : program.slug, season : season.id, episode_slug : episode.slug }) }}">Episode {{ episode.number }} : {{ episode.title }}</a>
        </li>
        {% else %}
            Aucun épisode trouvé.
        {% endfor %}
    </ul>
    <a href="{{ path('program_show', { slug : program.slug }) }}">
        Retour au program
    </a> 

{% endblock %}
