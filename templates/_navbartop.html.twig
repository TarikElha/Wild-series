{% for category in categories %}
        <li>
            <a href="{{ path('category_show', { categoryName : category.name|lower }) }}">
                {{ category.name }}
            </a>
        </li>
{% endfor %}